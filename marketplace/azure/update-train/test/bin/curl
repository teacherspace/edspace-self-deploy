#!/usr/bin/env bash
set -euo pipefail

url=""
for arg in "$@"; do
  case "$arg" in https://*) url=$arg ;; esac
done

case "$url" in
  */health) printf '200' ;;
  */version) printf '%s' "${MOCK_VERSION:-build-newsha}" ;;
  *) exit 2 ;;
esac
